package jks.amain;

/** The platform the launcher runs the game on (r135). Set before anything reads the config. */
public class GVars_Platform
{
	public static Platform current = new Platform() {} ;
}
